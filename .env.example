# DevBrain AI – Frontend
# Point to your Ragpi API (no trailing slash).

# —— Local dev (avoids CORS): use /api so Vite proxies to your backend ——
# The dev server proxies /api/* to VITE_PROXY_TARGET (default: https://malaysiantradenets.com).
# Restart dev server after changing.
VITE_API_BASE_URL=/api
# Optional: override proxy target (default: https://malaysiantradenets.com)
# VITE_PROXY_TARGET=https://malaysiantradenets.com

# —— Or use backend URL directly (requires CORS on Ragpi for your frontend origin) ——
# VITE_API_BASE_URL=https://malaysiantradenets.com
# VITE_API_BASE_URL=https://185.216.203.209:8001
# VITE_API_BASE_URL=http://localhost:8001

# —— Admin panel: user with this email can access /admin/* ——
# VITE_ADMIN_EMAIL=admin@example.com
